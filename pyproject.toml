[project]
name = "atu-bot"
version = "1.0.0"
description = "Telegram bot for ATU timetable"
requires-python = ">=3.11"

[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP"]
